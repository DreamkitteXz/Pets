name: pet_app
description: A new Flutter project.
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.1+6

environment:
  sdk: '>=2.19.6 <3.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2
  firebase_core: ^2.15.0
  cloud_firestore: ^4.8.3
  firebase_auth: ^4.7.0
  get: ^4.6.5
  # 'auth' (social login: facebook/twitter/google) removido: não era usado no
  # código e fixava http ^0.13 (conflito com package_info_plus 10) + causava os
  # problemas de JVM-target (flutter_facebook_auth) e namespace (twitter_login).
  mask_text_input_formatter: ^2.5.0
  uuid: ^4.5.1
  google_fonts: ^4.0.4
  shared_preferences: ^2.2.0
  provider: ^6.1.2
  from_css_color: ^2.0.0
  intl: ^0.18.1
  font_awesome_flutter: ^10.4.0
  auto_size_text: ^3.0.0
  json_path: ^0.5.2
  timeago: ^3.5.0
  url_launcher: ^6.1.11
  page_transition: ^2.0.9
  cached_network_image: ^3.2.3
  latlng: ^1.0.0
  flutter_typeahead: ^4.6.2
  email_validator: ^2.1.17
  firebase_storage: ^11.2.5
  image_picker: ^1.0.1
  signature: ^5.4.0
  flutter_animate: ^4.2.0+1
  dropdown_button2: ^2.1.4
  flutter_native_splash: ^2.2.19
  flutter_launcher_icons: ^0.13.1
  pdf: ^3.11.1
  open_file: ^3.3.2
  path_provider: ^2.1.1
  syncfusion_flutter_pdf: ^23.1.36
  flutter_svg: ^2.0.9
  carousel_slider: ^5.1.2
  smooth_page_indicator: ^1.1.0
  permission_handler: ^11.0.1
  package_info_plus: ^10.1.0
  path: ^1.8.3
  percent_indicator: ^4.2.4
  geolocator: ^14.0.0
  fl_chart: ^1.0.0
  dropdown_search: ^5.0.6
  async: ^2.11.0
  cloud_functions: ^4.7.6
  # SHA-256 do APK baixado (fluxo de atualizacao fora da Play Store).
  crypto: ^3.0.3
  
flutter_native_splash:
  color: "#041A23"
  image: "assets/app/pets_logo.png"
  android: true
  ios: true

flutter_launcher_icons:
  image_path: "assets/app/logo.png"
  ios: true
  # iOS recusa icone com canal alfa; a logo tem fundo transparente.
  remove_alpha_ios: true
  # `ic_launcher` e o nome que o AndroidManifest referencia. Antes gerava
  # `launcher_icon`, que ninguem usava — por isso o icone continuava o padrao
  # do Flutter mesmo com a config no lugar.
  android: "ic_launcher"
  # Adaptive icon (Android 8+): o sistema recorta o primeiro plano em varias
  # formas. Fundo branco porque a logo tem traco escuro e transparencia.
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/app/logo.png"

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Publicacao de release (tool/release.dart). Ficam em dev_dependencies de
  # proposito: sao usadas so pelo script, nunca entram no APK.
  googleapis_auth: ^1.4.1
  http: ^1.1.0
  yaml: ^3.1.2

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - lib/assets/welcome_pet.png
    - lib/assets/cat-removebg-preview.png
    - lib/assets/catfemea-removebg-preview.png
    - lib/assets/dog-removebg-preview.png
    - lib/assets/dogfemea-removebg-preview.png
    - lib/assets/pet.png
    - lib/assets/vacinadog-removebg-preview.png
    - lib/assets/vacinadogmacho-removebg-preview.png
    - lib/assets/catfemeavac-removebg-preview.png
    - lib/assets/catmachovac-removebg-preview.png
    - lib/assets/vermifugo.png
    - lib/assets/vacinabk.png
    - lib/assets/animation_lkreu8c0_small.gif
    - assets/app/icon_png.png
    - lib/assets/OpenSans-Regular.ttf
    - assets/app/pets_logo.svg
    - assets/app/pets_logo.png
    - lib/screens/assets/cat1.svg
    - lib/screens/assets/cat2.svg
    - lib/screens/assets/dog1.svg
    - lib/screens/assets/password_icon.svg
    - lib/screens/assets/bubbles_snack_bar.svg
    - lib/screens/assets/close_snack_bar.svg
    - lib/screens/assets/fail_snack_bar.svg
    - lib/screens/assets/pata.png
    - lib/screens/assets/color.png
    - lib/screens/assets/raca.png
    - lib/screens/assets/tipo.png
    - lib/screens/assets/sexo.png
    - lib/screens/assets/docs.svg
    - lib/screens/assets/validado.svg
    - lib/screens/assets/aguardando.svg
    - assets/config/
    - assets/images/defaults/
    - assets/images/defaults/dogs/
    - assets/images/defaults/dogs/beagle/
    - assets/images/defaults/dogs/pintcher/
    - assets/images/defaults/dogs/golden/
    - assets/images/defaults/cats/
    - assets/images/defaults/cats/somali/
    - assets/images/defaults/dogs/beagle/male/
    - assets/images/defaults/dogs/pintcher/male/
    - assets/images/defaults/dogs/golden/male/
    - assets/images/defaults/cats/somali/male/
    - assets/images/defaults/dogs/boxer/
    - assets/images/defaults/dogs/boxer/male/
    - assets/images/defaults/dogs/chihuahua/
    - assets/images/defaults/dogs/chihuahua/male/
    - assets/images/defaults/dogs/dachshund/
    - assets/images/defaults/dogs/dachshund/male/
    - assets/images/defaults/dogs/german_shepherd/
    - assets/images/defaults/dogs/german_shepherd/male/
    - assets/images/defaults/dogs/husky/
    - assets/images/defaults/dogs/husky/male/
    - assets/images/defaults/dogs/maltese/
    - assets/images/defaults/dogs/maltese/male/
    - assets/images/defaults/dogs/pitbull/
    - assets/images/defaults/dogs/pitbull/male/
    - assets/images/defaults/dogs/poodle/
    - assets/images/defaults/dogs/poodle/male/
    - assets/images/defaults/dogs/pug/
    - assets/images/defaults/dogs/pug/male/
    - assets/images/defaults/dogs/rottweiler/
    - assets/images/defaults/dogs/rottweiler/male/
    - assets/images/defaults/dogs/zwergspitz/
    - assets/images/defaults/dogs/zwergspitz/male/
    - assets/images/defaults/dogs/bulldog/
    - assets/images/defaults/dogs/bulldog/male/
    - assets/config/pet_assets.json
    - assets/images/pet_owner.png
    - assets/images/vacine.jpeg
    - shorebird.yaml

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  fonts:
    - family: Montserrat
      fonts:
        - asset: lib/assets/OpenSans-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
    - family: Inter
      fonts:
        - asset: assets/fonts/inter.ttf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
